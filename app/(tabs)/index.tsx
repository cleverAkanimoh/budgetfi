import { View, Text } from "react-native";
import React from "react";
import { NetflixIcon } from "@/constants/icons";

const Page = () => {
  return (
    <View>
      <Text>Home</Text>
      <NetflixIcon />
    </View>
  );
};

export default Page;
